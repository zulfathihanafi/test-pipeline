name: Deploy Staging

on:
  workflow_call:

jobs:
  deploy:
    runs-on: ubuntu-latest
    environment: staging

    steps:
      - uses: actions/checkout@v4

      - name: Deploy to Staging
        env:
          APP_ENV: ${{ vars.SECRET_URL }}
          DB_HOST: ${{ secrets.DB_HOST }}
        run: |
          echo "Deploying to STAGING"
          echo ${APP_ENV}
          echo ${DB_HOST}
